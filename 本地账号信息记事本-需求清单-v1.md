# 本地账号信息记事本需求清单

- 版本：v1.0
- 整理日期：2026-07-22
- 当前状态：2026-07-28 已实现可选启动锁、数据库与可选图片文件加密、分类右键、同步与安卓前置数据模型，并完成 v0.2.0 绿色版发布验收

## 1. 产品定位

本产品是一款仅在 Windows 本地运行的便携式账号信息记事本，用于集中记录软件、网站及其多个账号的相关资料。

产品以体积小、打开即用、记录方便、查找快捷和数据易迁移为主要目标，不定位为高安全等级密码管理器。

### 1.1 核心原则

1. 仅支持 Windows 10 及以上系统。
2. 提供绿色便携版，不要求安装，不依赖管理员权限。
3. 一台电脑按一个本地用户使用。
4. 默认启动后直接进入记录页；用户可以在设置中选择启用本机启动密码。
5. 不维护后端服务，不绑定邮箱，不提供云同步及邮箱找回功能。
6. 数据和图片保存在本地，优先保证使用方便、数据完整和迁移方便。
7. 不承诺防御能够直接读取本机文件的其他用户或恶意软件。

## 2. 信息组织结构

信息采用“软件或网站分区 → 多个独立账号”的两层结构。

### 2.1 软件或网站分区

每个分区用于表示一个软件、游戏平台、网站或服务，包含：

- 名称；
- 自定义图标；
- 官网或登录页面超链接；
- 一个层级分类；
- 多个标签；
- 创建时间和最后修改时间。

同一个分区下可以创建多个账号。不同账号的数据相互独立，修改或删除一个账号时不得影响其他账号。

### 2.2 账号记录

每个账号默认支持以下内容：

- 账号名称或用户名；
- 当前密码；
- 多组密保问题及回答；
- 很长的身份识别码、恢复码或其他长文本；
- 普通备注；
- 可点击的文本超链接；
- 多张图片；
- 自定义字段；
- 最近三个历史密码；
- 创建时间和最后修改时间。

### 2.3 自定义字段

用户可以为单个账号添加任意数量的自定义字段。

每个自定义字段至少包含：

- 字段名称；
- 字段内容；
- 单行或多行显示方式；
- 是否显示快速复制按钮。

自定义字段用于记录手机号、邮箱、服务器地址、许可证、备用恢复码、注册日期等非固定信息。

## 3. 分类与标签

### 3.1 分类

1. 每个软件或网站分区只能选择一个分类。
2. 分类支持层级结构，例如“工作 → 设计工具”或“游戏 → 游戏平台”。
3. 用户可以新增、重命名、移动和删除分类。
4. 删除仍在使用的分类时，应要求用户选择新的分类或将相关分区移到“未分类”。

### 3.2 标签

1. 每个软件或网站分区可以选择多个标签。
2. 用户可以新增、重命名和删除标签。
3. 每个标签支持自定义颜色。
4. 软件列表支持按一个或多个标签筛选。
5. 删除标签只解除关联，不删除软件分区及账号数据。

## 4. 图片功能

1. 每个账号可以保存多张图片。
2. 支持直接从系统剪贴板粘贴图片。
3. 支持从本地选择常见图片文件。
4. 支持粘贴图片网址并显示图片预览。
5. 图片网址需要保留原始链接；成功加载后应缓存本地副本，避免原链接失效后图片立即丢失。
6. 支持常见格式，至少包括 PNG、JPG、JPEG、WebP、GIF 和 BMP。
7. 图片支持缩略图查看、原图查看、复制、替换和删除。
8. 图片属于具体账号，不与同一软件分区下的其他账号混用。
9. 暂不支持 PDF、压缩包、文档、视频等非图片附件。

## 5. 密码修改与历史记录

### 5.1 更新确认

当用户修改当前密码并准备保存时，程序必须识别到密码字段发生变化，并提供以下三个选择：

1. **取消更新**：返回编辑状态，不保存本次密码修改；
2. **更新并保留旧密码**：保存新密码，并将被替换的当前密码加入历史记录；
3. **更新但不保留旧密码**：保存新密码，但不把被替换的密码加入历史记录。

这样可以避免一次误输入或误粘贴被直接识别为正式密码更新。

### 5.2 历史密码规则

1. 每个账号最多保存最近三个不同的历史密码。
2. 历史密码按最近被替换的时间排序。
3. 当前密码不得同时出现在历史密码列表中。
4. 准备加入的旧密码如果已存在于历史记录中，不新增重复项，并将其视为最近使用的历史密码。
5. 新密码如果与某个历史密码相同，该密码从历史列表中移除，因为它已经重新成为当前密码。
6. 超过三个不同历史密码时，自动删除最早的一项。
7. 用户可以查看、复制或手动删除某条历史密码。
8. 历史密码不允许直接覆盖当前密码；如需恢复，应通过明确的“设为当前密码”操作，并再次触发更新确认。

## 6. 链接跳转与快速复制

1. 软件或网站分区支持设置一个默认登录网址。
2. 备注和自定义字段中的网址可以显示为可点击超链接。
3. 点击链接时调用 Windows 默认浏览器打开。
4. 账号、当前密码、历史密码、身份识别码以及启用了复制按钮的自定义字段均支持一键复制。
5. 不自动填写网页表单，不自动登录网站，不开发浏览器扩展。
6. 不要求复制后自动清空剪贴板。
7. 不要求全局快捷键、密码定时隐藏或其他安全型交互。

用户的典型登录流程为：打开对应分区 → 点击超链接进入网站 → 复制账号和密码 → 手动粘贴登录。

## 7. 查找、筛选与浏览

1. 支持按软件或网站名称、账号名称、身份识别码、备注、自定义字段、分类和标签进行搜索。
2. 支持按分类树浏览软件分区。
3. 支持按多个标签筛选软件分区。
4. 支持组合使用分类、标签和关键词。
5. 搜索结果应显示所属软件分区和账号，避免同名账号无法区分。
6. 图片内容不做 OCR 文字识别。
7. 暂不提供收藏和归档功能。

## 8. 编辑与误操作处理

1. 新增、编辑和删除软件分区及账号时，操作入口应清晰且一致。
2. 编辑内容采用明确的保存操作。
3. 关闭编辑页或退出程序时，如存在未保存修改，应提示保存、放弃或取消关闭。
4. 删除账号或软件分区前必须二次确认。
5. 删除软件分区时，应明确提示其下包含的账号和图片数量。
6. 当前版本不要求回收站和误删恢复。

## 9. 修改日志

1. 程序在本地维护简洁、可读的修改日志。
2. 日志至少记录：操作时间、软件分区、账号、操作类型以及发生变化的字段名称。
3. 操作类型至少包括新增、修改、删除、导入和恢复备份。
4. 密码变化只记录“密码已修改”以及是否保留历史密码，不在日志中重复写入密码全文。
5. 图片变化只记录新增、替换或删除及图片名称，不记录图片二进制内容。
6. 日志文件总大小达到 10 MB 后，自动从最早的记录开始清理，直到文件重新低于限制。
7. 清理日志不得影响正式数据、图片和密码历史。
8. 程序内提供“打开日志文件”入口，便于直接查看。

## 10. 本地数据与便携性

1. 程序、数据和图片应组成一个可整体移动的便携目录。
2. 默认将数据保存在程序目录下的独立 `data` 文件夹中。
3. 用户将整个程序目录复制到另一台 Windows 10 以上电脑后，应可以直接继续使用。
4. 账号数据使用认证加密信封保存；未启用启动密码时保留本机自动解锁能力。
5. 图片不得只保存临时路径；粘贴或导入后必须复制到程序自己的数据目录。
6. 数据库记录与图片文件之间需要保持稳定关联，移动程序目录后不得失效。
7. 程序升级不得覆盖用户数据。

## 11. 导入、导出与备份

### 11.1 完整备份包

1. 提供应用自有的完整备份格式。
2. 完整备份必须包含软件分区、账号、分类、标签、自定义字段、图片、历史密码和必要配置。
3. 支持从完整备份包恢复，并在恢复前展示内容摘要。
4. 完整备份和恢复必须保证图片信息不丢失。
5. 完整备份是跨电脑迁移和灾难恢复的首选方式。

### 11.2 Excel

1. 支持导出为 `.xlsx`。
2. 表格中完整保留可结构化的文字字段。
3. 图片预览嵌入可见工作表，原始图片字节分块保存在隐藏工作表中，不依赖配套目录。
4. 支持导入程序自己导出的标准 Excel 模板。
5. 不解析任意来源或任意列结构的 Excel；非本程序标准模板必须明确拒绝。

### 11.3 Word

1. 支持导出为 `.docx`，用于查看、打印或人工整理。
2. Word 导出内容应包含账号文字信息和对应图片。
3. Word 只用于查看和打印，不提供 Word 导入或任意排版文档解析。

### 11.4 导入规则

1. 当前导入采用完整数据替换，不做按名称合并；预览显示数据版本、文件时间、分类、标签、分区、账号和图片数量。
2. 用户确认替换前必须自动创建加密回滚备份；回滚备份失败时不开始写入。
3. 导入数据必须通过版本、编号、引用、图片可移植性和同步元数据校验；任何校验或保存失败均不得破坏已有数据。
4. 导入完成后显示恢复数量和回滚备份路径；失败时显示失败阶段及可用回滚路径。

## 12. 软件图标

1. 每个软件或网站分区支持自定义图标。
2. 支持从本地图片选择或从剪贴板粘贴图标。
3. 图标文件复制到程序数据目录，不依赖原文件位置。
4. 未设置图标时使用统一默认图标。
5. 第一版不要求自动联网获取网站图标。

## 13. 界面与体验要求

1. 界面以紧凑、清晰和快速操作为主，不采用复杂的管理后台布局。
2. 主要界面应同时便于浏览分类、选择软件分区和查看账号详情。
3. 多账号之间需要有明显边界，避免复制错账号或修改错记录。
4. 账号和密码复制按钮应始终容易找到。
5. 长身份识别码和长备注不得被强制截断，应支持换行和展开查看。
6. 图片缩略图不得挤压主要文字信息。
7. 标签颜色需要保持可读性，不能只依赖颜色表达标签含义。
8. 窗口关闭后程序直接退出，不要求驻留系统托盘。
9. 程序应保持较小体量和较快启动速度，避免引入非必要常驻服务。

## 14. 明确不做的功能

当前版本不包含：

- 网络账号、多用户系统和隐藏解锁后门；
- 邮箱绑定、邮箱验证和密码找回；
- Windows Hello、指纹或 PIN 解锁；
- 云同步、网络账号和后端服务；
- 浏览器扩展、网页自动填充和自动登录；
- 自动锁定、输错限制和剪贴板自动清理；
- 非图片文件附件；
- 收藏、归档和回收站；
- 图片 OCR；
- 自动联网获取软件图标；
- 面向本机恶意软件或其他本地用户的数据防护承诺。

## 14.1 可选启动锁补充

1. 设置页提供“安全”标签，用户可选择启用启动密码。
2. 启用时生成一次性展示的 16 位恢复码，至少包含大写字母、小写字母、数字和特殊字符。
3. 启动密码和恢复码分别包装同一随机数据密钥，不在配置文件中保存明文凭据。
4. 使用恢复码解锁后明确提醒修改启动密码，并允许在已解锁状态直接修改。
5. 启动密码与恢复码同时丢失时数据不可恢复；不提供固定万能密码、隐藏配置开关或其他绕过方式。
6. 默认未启用启动锁时，自动解锁密钥用于维持打开即用，只防止直接打开数据库阅读，不承诺抵御整个程序目录被复制。

## 14.2 同步与安卓端预留

1. 同步接口只传输加密 vault 快照，不向同步服务发送明文账号数据。
2. 当前不实现服务器、网络账号或实际同步。
3. 安卓端建议使用 Kotlin、Jetpack Compose 和 Room，复用版本化数据 DTO 与加密信封协议，不直接复用桌面 SQLite 文件。
4. AppData v5 为分类、标签、分区、账号和设置记录 revision、更新时间及修改设备，并使用删除墓碑防止旧设备复活已删除内容。
5. `data/device.json` 保存随机本机设备 ID 和最近成功同步游标，不进入完整备份，不包含密码、恢复码、数据密钥或账号正文。
6. 快照 v2 使用快照 ID、父快照 ID、vault revision 和条件推送前置值；本地与远端同时修改时停止覆盖，等待未来的人工冲突处理界面。
7. 完整备份保留同步 lineage；Excel 导回建立新 lineage，不携带删除历史或伪造同步游标。

## 14.3 可选图片文件加密

1. 设置页“安全”标签提供“加密本地图片文件”开关，默认关闭，用户确认应用后才迁移已有图片。
2. 开启后，`data/images` 和 `data/icons` 中的文件不能被图片查看器直接打开；软件内显示、图片预览、完整备份及 Excel/Word 导出保持可用。
3. 图片使用与账号数据库相同的随机数据密钥和带认证加密，不引入第二套密码或密钥。
4. 开启或关闭时保留原文件名和记录路径；单文件替换失败时原文件必须保持不变，密文损坏或密钥不匹配必须明确报错。
5. 旧版数据升级后保持关闭状态，不在首次启动时静默批量处理图片。
6. 单张图片继续限制为 15 MB；迁移过程允许短暂占用约两倍原始字节内存，当前版本不额外建立缩略图缓存。
7. 未启用启动密码时，自动解锁密钥仍随便携目录保存；图片加密用于避免直接查看文件，不承诺抵御整个程序目录被复制后的专业提取。

## 15. 第一版验收重点

第一版至少满足以下闭环：

1. 绿色程序可以在 Windows 10 及以上系统直接启动。
2. 可以建立软件分区，并在同一分区内建立多个互不干扰的账号。
3. 可以保存账号、密码、密保问答、长识别码、备注、超链接、自定义字段和多张图片。
4. 可以设置图标、层级分类和彩色多选标签，并完成搜索和筛选。
5. 可以点击网站链接，并一键复制账号、密码和其他指定字段。
6. 修改密码时会出现三项确认，历史密码按照去重、排除当前密码和最多三个的规则更新。
7. 可以查看简洁的修改日志，日志超过 10 MB 后正确清理最早记录。
8. 复制整个便携目录到另一台电脑后，数据和图片仍能正常显示。
9. 完整备份和恢复不会丢失图片或账号层级关系。
10. Excel 和 Word 按标准模板导出后，文字与图片关联清晰；程序支持导回自己生成的标准模板。
11. 可选图片加密开启后，本地图片文件不能直接打开，软件内预览和导出不受影响；关闭后恢复原始图片字节。

## 16. 后续想法：默认密码格式模板

状态：已完成（2026-07-27），位于“设置 > 密码模板”。

### 16.1 使用目标

1. 用户可以在设置页保存一个自己习惯使用的默认密码结构。
2. 典型结构由固定大小写英文、特殊符号、数字和需要按网站填写的特定名称组成。
3. 模板应提供明确的网站名称占位位置，例如：`固定英文!固定数字{网站名称}`。
4. 创建或修改账号密码时，用户可以点击一次“填充密码模板”，将该结构快速写入密码输入框。
5. 填充后仍允许自由编辑，用户只需补充或替换网站特定名称即可形成最终密码。
6. 如果用户没有在设置页配置密码模板，密码输入框继续沿用当前的完全手动输入方式，不增加额外步骤。

### 16.2 行为边界

1. 该功能是用户自定义模板的快捷填充，不等同于随机密码生成器。
2. 模板填充只修改当前编辑表单的草稿，不应立即写入数据库。
3. 只有用户保存账号后，最终密码才按照现有规则处理，并在密码发生变化时询问是否保留旧密码。
4. 如果密码输入框中已经存在内容，快捷填充前必须二次确认，不得静默覆盖。
5. 密码模板属于本地应用配置，已纳入完整备份、恢复和 Excel 标准模板往返范围。
6. 当前先支持一个全局默认模板；是否需要为分类或软件分区设置不同模板，暂不扩展。
